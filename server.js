const { server } = require("./jcc");

server.run();
// const express = require("express");
// const app = express();
// app.use(express.json());
// app.get("/jcc", (req, res) => res.send(`<h3>url :${req.url}</h3>`));
// app.listen(5000, () => console.log("running on port 5000"));
